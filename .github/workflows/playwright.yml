name: Playwright Tests

on:
  workflow_dispatch:  # Allow manual triggering

jobs:
  test:
    timeout-minutes: 60
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: lts/*

      - name: Install dependencies
        run: npm ci

      - name: Install Playwright Browsers
        run: npx playwright install --with-deps

      - name: Run Playwright Tests (Desktop-Cart-Enabled)
        run: |
          echo "Running tests on ${{ secrets.ENVIRONMENT }} environment with chromium desktop"
          npx playwright test guest-cart-desktop-private-test.spec.ts --headed --project=chromium-desktop
        env:
          privateCartBaseUrl: ${{ secrets.PRIVATE_CART_BASE_URL }}
          environment: ${{ secrets.ENVIRONMENT }}

      - name: Run Playwright Tests (Mobile-Cart-Enabled)
        run: |
          echo "Running tests on ${{ secrets.ENVIRONMENT }} environment with mobile"
          npx playwright test guest-cart-mobile-private-test.spec.ts --headed --project=mobile-iphone-safari
        env:
          privateCartBaseUrl: ${{ secrets.PRIVATE_CART_BASE_URL }}
          environment: ${{ secrets.ENVIRONMENT }}

      - uses: actions/upload-artifact@v4
        if: ${{ !cancelled() }}
        with:
          name: playwright-report
          path: playwright-report/
          retention-days: 30
